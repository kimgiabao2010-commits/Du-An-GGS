import { Bot, Cpu, Zap } from 'lucide-react';
import { DataNotice, PageHeader, StatusBadge } from '../../components/ui';
import { agents } from '../../lib/operations-data';

export const metadata = { title: 'Agents' };
export default function AgentsPage() { return <div className="page-stack"><PageHeader eyebrow="Fleet" title="Agents" description="Capacity, specialization and recent behavior across the local agent runtime." /><DataNotice /><div className="agent-grid">{agents.map(agent => <article className="panel agent-panel" key={agent.id}><div className="agent-head"><span className="agent-avatar"><Bot size={20} /></span><div><h2>{agent.name}</h2><p>{agent.role}</p></div><StatusBadge status={agent.state} /></div><div className="capability-list">{agent.capabilities.map(capability => <span key={capability}>{capability}</span>)}</div><dl className="agent-stats"><div><dt>Current task</dt><dd>{agent.currentTask}</dd></div><div><dt>Success rate</dt><dd>{agent.successRate}</dd></div><div><dt>Tokens today</dt><dd>{agent.tokens}</dd></div></dl><div className="agent-activity"><Zap size={14} /><span>{agent.activity}</span></div></article>)}</div><section className="quiet-banner"><Cpu size={20} /><div><strong>Runtime boundary</strong><p>Fleet state is currently represented by a demo adapter. Live worker presence requires a durable backend state API.</p></div></section></div>; }
