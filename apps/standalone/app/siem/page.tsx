import { CircleAlert, Database, ExternalLink, Radar, ShieldCheck } from 'lucide-react';
import AgentStatusPanel from '../../components/AgentStatusPanel';
import ChartsGroup from '../../components/ChartsGroup';
import { DataNotice, PageHeader } from '../../components/ui';

export const metadata = { title: 'SIEM' };
export default function SiemPage() { return <div className="page-stack"><PageHeader eyebrow="Security observability" title="SIEM workspace" description="A transparent staging surface for Chronicle-bound search, signals and backend observer messages." actions={<button className="button secondary" disabled><ExternalLink size={15} />Open Chronicle</button>} /><DataNotice /><section className="siem-status-ribbon"><div><span className="siem-status-icon"><ShieldCheck size={20} /></span><span><strong>Adapter not configured</strong><small>Chronicle UDM Search credentials are required for live evidence.</small></span></div><span className="status-badge status-queued"><CircleAlert size={14} />Blocked safely</span></section><div className="siem-layout"><div className="siem-primary"><ChartsGroup /><section className="panel signal-list"><div className="section-heading"><div><p className="eyebrow">Signal inbox</p><h2>Review queue</h2></div><span className="subtle-label">2 demo signals</span></div><div className="signal-row"><span className="signal-icon critical"><Radar size={17} /></span><div><strong>Potential encoded payload on WS-044</strong><p>Representative high-priority signal. Not verified by a live SIEM adapter.</p></div><span className="severity-label critical">Critical</span></div><div className="signal-row"><span className="signal-icon info"><Database size={17} /></span><div><strong>Identity correlation pending</strong><p>Read-only query is blocked until Chronicle staging is configured.</p></div><span className="severity-label info">Info</span></div></section></div><aside className="siem-observer"><AgentStatusPanel title="Backend observer" /></aside></div></div>; }
